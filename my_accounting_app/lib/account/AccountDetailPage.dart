import 'package:flutter/material.dart';

class AccountDetailPage extends StatelessWidget {
  final int id;
  AccountDetailPage(this.id);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
